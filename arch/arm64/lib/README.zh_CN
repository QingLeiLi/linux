arch/arm64/lib 学习导读
=======================

这份导读解释本目录源码默认读者已经知道、但初学者通常不知道的背景。它和
各个 .S/.c 文件内的逐行注释配合阅读；这里只讲共性，不替代具体算法注释。

1. 这些代码为什么不用普通 C
----------------------------

这里实现内核最底层、调用频率极高的原语：清页、复制、字符串、校验和、用户
空间访问、MTE 标签操作等。汇编版本可以精确控制加载宽度、地址对齐、流水线、
异常修复和启动时指令替换。它们不是演示代码；许多看似绕远的写法是在同时满足：

* 对所有合法长度都不越过可访问页或 MTE granule；
* 在大小端、不同缓存行长度和不同 CPU 特性上语义一致；
* 用户地址在任意一条访存指令出错时，返回“尚未完成的字节数”而不是崩溃；
* 常见短输入启动开销小，长输入又能让多个 load/store 并行；
* memcpy 和 memmove 的重叠规则、KASAN/MTE 检查、内核替代指令机制均成立。

2. AArch64 调用约定和寄存器
-------------------------

普通 AAPCS64 C 函数用 x0..x7 传前八个整数/指针参数，x0（必要时连同 x1）
返回整数。x0..x18 多数是调用者保存，x19..x28 是被调用者保存，x29 是帧指针，
x30 是链接寄存器（返回地址），sp 是栈指针。叶函数不调用其他函数，因而常能把
x0..x17 都当临时寄存器使用而不建栈。wN 是 xN 的低 32 位；写 wN 会自动把 xN
高 32 位清零。xzr/wzr 读取恒为零，向它写入会丢弃结果。

许多文件用 ``name .req xN`` 或 ``#define name xN`` 给寄存器起语义别名。必须
跟踪“此刻寄存器代表什么”，而不能只记物理编号；优化代码会在某阶段结束后复用
寄存器。例如 memcpy.S 的 count 在尾部可能已成为普通数据寄存器。

3. 看懂常见指令写法
-------------------

``ldr x3, [x1], #8`` 是后索引：先从旧 x1 读 8 字节，再令 x1+=8。
``ldr x3, [x1, #8]!`` 是前索引：先令 x1+=8，再读取。``ldp/stp`` 一次加载/
存储两个寄存器；x 寄存器版本通常搬 16 字节，w 版本搬 8 字节。``ldrb/ldrh``
读 1/2 字节并零扩展。AArch64 一般允许普通内存的非对齐访问，但跨页、设备内存、
MTE granule 和性能仍会影响算法设计。

带 ``s`` 后缀的算术指令（subs/ands/bics）会更新 NZCV 条件码。cmp 是丢弃结果的
subs，tst 是丢弃结果的 ands。常见条件：eq/ne 看 Z，lo/hs（或 cc/cs）表示无
符号小于/不小于，lt/ge 表示有符号小于/不小于，hi/ls 表示无符号大于/不大于，
mi/pl 看结果负号。``ccmp`` 只在条件成立时真比较，否则直接把立即数写入 NZCV，
可把两个逻辑判断合成一条无分支条件链。

``tbz/tbnz reg,#bit,label`` 测一个位；按长度的二进制位拆分尾部时尤其常见，例如
依次测试 8、4、2、1 就能恰好处理任意 0..15 字节。数字标签是局部标签：``1b``
跳到向后最近的 1，``2f`` 跳到向前最近的 2。``.p2align n`` 按 2^n 字节对齐
后续热点循环，以减少取指跨缓存行。

4. 为什么有重叠读取/写入
-----------------------

memcpy/memset 的短路径常“从开头取一块，再从结尾取一块”。例如长度 9..16 时，
读取 [src,0..7] 和 [src+n-8..n-1]。中间可能重叠，但并未越过请求范围，而且能用
固定两次访存代替多分支尾循环。存储相同：同一字节写两次相同值没有问题。

这不等于随意越界。字符串函数不知道对象精确长度，有些宽加载可能读到 NUL 之后，
但必须保证不跨入不可访问页；启用硬件标签 KASAN 时甚至不能跨 16 字节 MTE
granule。源码中的 page_cross、对齐下取整和掩码路径就是在维护这条安全边界。

5. 并行零字节检测
-----------------

字符串和 memchr 的核心公式是：

  has_zero = (x - 0x0101010101010101) & ~(x | 0x7f7f7f7f7f7f7f7f)

若某字节为 0，对应槽位会出现最高位标记，因此一次 64 位运算能检查八个字节。
寻找字符 c 时先把 c 广播八份并与 x 异或，相等的槽就变成 0，再套同一公式。
找到含候选的机器字后，rev 配合 clz 把“内存地址最靠前的标记”转换为字节索引。
大小端代码不同，主要因为寄存器的最高有效字节与最低内存地址的对应关系不同，
以及逐字节减法的借位传播方向不同。

6. 用户空间访问和异常表
----------------------

内核不能相信用户指针一直有效：页可能未映射、权限不足，甚至复制途中被另一线程
改变映射。asm-uaccess.h 的 USER/user_ldst/user_ldp/user_stp 宏把“可能 fault
的指令地址 -> 修复标签”登记到异常表。正常执行时这些宏几乎就是 ldtr/sttr；
发生同步异常时，异常处理器改写 PC 跳到 9997/9998 等修复标签。修复代码根据已经
推进的 src/dst/count 计算未完成字节数。ldtr/sttr 是 unprivileged 访问形式，让
EL1 按非特权权限检查用户地址。不要把数字 999x 当普通控制流入口。

copy_template.S 是文本模板，不独立编译。copy_from_user.S 与 copy_to_user.S 先
定义同名 load/store 宏：用户侧那一半带异常表，内核侧只是普通访存，然后 include
同一复制算法。这样两方向的性能结构一致，而 fault 责任恰好落在用户访存上。

7. MOPS、DC ZVA 与 alternatives
-----------------------------

FEAT_MOPS 提供 setp/setm/sete 和 cpyp/cpym/cpye 三指令序列：P/M/E 分别是
prologue/main/epilogue。带 ``!`` 的地址和长度寄存器由硬件更新；发生异常时架构
还规定寄存器的恢复格式，uaccess 修复代码中的“Option A”即指该规定。

``alternative_if_not ARM64_HAS_MOPS`` 不是运行时每次测试。内核启动时根据 CPU
能力把预留指令区改写为 MOPS 路径或跳转通用路径，热路径不承担特性分支成本。
``CONFIG_AS_HAS_MOPS`` 只说明构建用汇编器认识语法，不等于运行 CPU 一定支持。

``dc zva,address`` 将一个硬件规定大小的对齐块清零。DCZID_EL0 的 DZP 位说明
是否禁止，BS 字段给出块大小 4<<BS。MTE 的 ``dc gzva`` 同时清数据和 allocation
tags。它们类似“缓存行清零”但块长由寄存器报告，不应硬编码成 64。

8. MTE 标签是什么
------------------

Memory Tagging Extension 每 16 字节内存 granule 保存一个 4 位 allocation tag，
指针高位携带 logical tag；检查模式下两者不等会 fault。普通 ldr/str 搬数据而不搬
标签。ldg/stg 操作单个标签，ldgm/stgm 批量打包/展开多个标签，stz2g 同时把两个
granule 的数据清零并写标签。ptrace 的 PEEKMTETAGS/POKEMTETAGS 接口则把每个
4 位标签表示为用户缓冲区中的一个字节，所以 mte_copy_tags_* 两侧步长分别是
1 字节和 MTE_GRANULE_SIZE。

9. 各文件阅读路线
-----------------

建议先看 strchr.S、strrchr.S、tishift.S；再看 clear_page.S、copy_page.S、
memchr.S、memcmp.S；接着看 memset.S、memcpy.S。strlen/strcmp/strncmp/strnlen
把零检测、页边界、大小端和对齐拼在一起，难度最高。用户访问应按
copy_from_user.S -> copy_template.S -> copy_to_user.S -> clear_user.S 阅读。
mte.S 和 kasan_sw_tags.S 需要结合上面的标签与非标准调用约定背景。

C 文件中，csum.c 是 Internet checksum 的一补数加法优化；accumulate 把第 65
位进位折回低 64 位。delay.c 用体系计数器而不是假设某条空循环耗时固定。
insn.c 是“32 位 A64 指令字”的编码/解码工具箱，按掩码保留 opcode、插入立即数
或寄存器字段，并为动态代码修改、kprobe 和跳转生成服务。error-inject.c 修改保存
现场的 PC 来跳过被探测函数；uaccess_flushcache.c 只清理实际复制成功的目的范围。

10. 阅读时始终追问的四件事
--------------------------

* 当前指针指向“本块开头、下一块”还是因预偏置而指向更后位置？
* 当前 count 是原长度、剩余长度，还是为了复用条件分支而故意带负偏差？
* 最近哪条带 s 的指令写了 NZCV，中间指令是否保留它，后续分支按有符号还是无
  符号解释？
* 宽访存覆盖的首尾地址是什么；它可能重叠，但是否始终留在合法页/granule/
  用户已验证范围内？

能持续回答这四个问题，就不是在逐字翻译汇编，而是在按实现者的方式验证它。
